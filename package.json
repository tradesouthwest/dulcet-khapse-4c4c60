{
  "name": "healthy-work-habits-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp manifest.json dist/ && cp *.html dist/ && cp *.js dist/ && cp *.css dist/ && cp -r icons dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}}
